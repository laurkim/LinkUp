<h1>Event Index</h1>

<div class="events_form">
  <h4>Event Form</h4>

  <%= form_tag events_path do %>
    <p>
      <%= label_tag "Search Keyword: " %>
      <%= text_field_tag :'event[search_keyword]' %>
    </p>

    <p>
      <%= label_tag "Zipcode: " %>
      <%= text_field_tag :'event[zipcode]' %>
    <p>

    <p>
      <%= label_tag "Radius (Miles): " %>
      <%= text_field_tag :'event[radius]' %>
    <p>

    <p>
      <%= label_tag "Only Free Events?: " %>
      <%= check_box_tag :'event[free]' %>
    <p>

    <p>
      <%= label_tag "Start Date: " %>
      <%= select_tag :'event[start_date]', options_for_select(@timing), include_blank: true %>
    <p>

    <p>
      <%= submit_tag "Submit" %>
    <p>
  <% end %>
</div>
